LOY TRAVEL v0.9.1 — PACK OPEN HOTFIX

REPLACE ON maui-next:
- app.js
- styles.css

NO SQL CHANGES.

FIXES
- Open My Pack now gives immediate feedback instead of silently returning.
- Traveler matching is case/space tolerant.
- If Supabase data is still loading, the app retries connection.
- If Supabase rejects the save, the actual error is shown on-screen with Try Again.
- Pack cards are only revealable after the unlocks have been saved successfully.

Leave everything else alone.
