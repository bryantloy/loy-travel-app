LOY TRAVEL v0.9.2 — NAME-BASED PACK HOTFIX

WHY
The screenshot confirmed the app could not resolve Brooks's traveler UUID.
Pack ownership should not depend on that lookup.

RUN THIS SQL ONCE:
- SUPABASE-v092.sql

THEN REPLACE ON maui-next:
- app.js

styles.css does not need to change for this fix.

WHAT CHANGED
- Starter packs are now keyed by the selected profile name (Bryson/Bennett/Brooks).
- Existing traveler UUID is saved when available, but is no longer required.
- Pack unlocks and claims persist across devices exactly as before.
- The one-pack-per-boy rule remains enforced.

NO CARD ASSETS OR OTHER FILES NEED CHANGES.
